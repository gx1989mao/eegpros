function [ps] = spectrogrambjp(input,win_width,ford,Fs)

ps = zeros(,)


    [mgs,f]=marginalSpec(input,Fs);
    mgs =  medfilt1(mgs,5);
    mgs(mgs<1e-6)=1e-4;
    mgs = 10*log10(abs(mgs.^2)/length(f))/2; %单边谱 所以除以二 统一做成双边谱


end

